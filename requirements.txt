# Third-party dependencies for archiveTree.
#
# archive_to_s3.py / restore_from_s3.py / list_object_status.py:
boto3>=1.43

# archive_to_globus.py / restore_from_globus.py / globus_auth.py / globus_transfer.py:
globus-sdk>=3.41

# Optional (all scripts fall back to plain progress messages if absent, via
# try/except ImportError) but recommended for progress bars during
# inventory/tar/transfer/upload/download steps:
tqdm>=4.70

# Optional (archive_to_s3.py falls back to size-only upload verification if
# absent, via try/except ImportError) but recommended: lets archive_to_s3.py
# compute CRC64NVME checksums locally to verify uploads against S3's
# whole-object checksums before deleting source files.
awscrt>=0.23
